import React from "react";

const StakeUnstake = () => {
    const handleStake = () => {
        // staking here COME BACK TO THIS
    };

    const handleUnstake = () => {
        // how to unstake will go here
    };

    return (
        <div>
            <h2>Stake / Unstake</h2>
            <button onClick={handleStake}>Stake It</button>
            <button onClick={handleUnstake}>Unstake It</button>
        </div>
    );

};

export default StakeUnstake;




